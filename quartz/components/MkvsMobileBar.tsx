import type { QuartzComponent } from "./types"
import script from "./mkvs-mobile-bar.inline"

// Верхняя панель телефона: Проводник, тема, поиск и оглавление в одной
// закреплённой строке.
//
// Компонент сам ничего не рисует. Всё нужное на странице уже есть, но лежит в
// трёх разных местах разметки: «гамбургер» Проводника и поиск — в левой
// колонке (на телефоне она и есть верхняя панель), переключатель темы — в
// панели над текстом, оглавление — в правой колонке. Скрипт на узком экране
// переносит два последних узла в левую колонку, чтобы все четыре кнопки стали
// обычными элементами одной flex-строки: порядок и отступы тогда задаются
// стилями, а не подгонкой координат у трёх независимо закреплённых кнопок.
// Закрепление панели и вид кнопок — в custom.scss, п.12.
const MobileBar: QuartzComponent = () => null

MobileBar.displayName = "MkvsMobileBar"
MobileBar.afterDOMLoaded = script
export default MobileBar
